
1 -Como se utiliza o comando ps para:
(a) Mostrar todos os processos rodando na máquina?

use o comando ps

(b) Mostrar os processos de um usuário?

$ps -u usuario

(c) Ordenar todos os processos de acordo com o uso da CPU?

 ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%cpu | head

(d) Mostrar a quanto tempo cada processo está rodando?

ps -e -o pid,comm,etime 

2- De onde vem o nome fork()?

vem de bifurcacao

3- Quais são as vantagens e desvantagens em utilizar:

(a) system()?

Porque com ele, voce pode mandar um comando no terminal dentro de seu codigo.

(b) fork() e exec()?

Com o fork e possivel criar copias de seu processo, podendo modifica-las de forma individual em paralelo. Porem, tal procedimento pode
se tornar pergigoso, criando v[arias c[opias ate que se perca o controle.

4- É possível utilizar o exec() sem executar o fork() antes?

5- Quais são as características básicas das seguintes funções:

(a) execp()?

(b) execv()?

(c) exece()?

(d) execvp()?

(e) execve()?

(f) execle()?
